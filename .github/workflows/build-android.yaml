name: Build Android

on:
  workflow_call:
    inputs:
      version:
        required: true
        type: string
      flutter_version:
        required: true
        type: string
    secrets:
      KEYSTORE_BASE64:
        required: true
      KEYSTORE_PASSWORD:
        required: true
      KEY_PASSWORD:
        required: true
      KEY_ALIAS:
        required: true

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: ${{ inputs.flutter_version }}

      - name: Get Dependencies
        run: |
          flutter --version
          flutter pub get

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'
          check-latest: true

      - name: Setup Android signing
        run: |
          echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > android/app/keystore.jks        
          echo "storePassword=${{ secrets.KEYSTORE_PASSWORD }}" > android/key.properties    
          echo "keyPassword=${{ secrets.KEY_PASSWORD }}" >> android/key.properties    
          echo "keyAlias=${{ secrets.KEY_ALIAS }}" >> android/key.properties    
          echo "storeFile=keystore.jks" >> android/key.properties

      - name: Build for Android
        run: |
          flutter gen-l10n
          dart run build_runner build --delete-conflicting-outputs

          flutter pub get

      - name: Increment Android versionCode
        run: |
          LOCAL_PROPS="android/local.properties"
          CURRENT_VERSIONCODE=$(grep "versionCode" $LOCAL_PROPS | grep -o '[0-9]\+' | head -1)
          NEXT_VERSIONCODE=$((CURRENT_VERSIONCODE + 1))
          sed -i "s/flutter.versionCode=$CURRENT_VERSIONCODE/flutter.versionCode=$NEXT_VERSIONCODE/" $LOCAL_PROPS
          echo "Incremented versionCode to $NEXT_VERSIONCODE"
          cat $LOCAL_PROPS
        shell: bash

      - name: Build APKs
        run: |
          flutter build apk --release
          flutter build apk --split-per-abi

      - name: Rename builds
        run: |
          cd build/app/outputs/flutter-apk
          mv app-armeabi-v7a-release.apk Cubebook-android-${{ inputs.version }}-armeabi-v7a.apk
          mv app-arm64-v8a-release.apk Cubebook-android-${{ inputs.version }}-arm64-v8a.apk
          mv app-x86_64-release.apk Cubebook-android-${{ inputs.version }}-x86_64.apk
          mv app-release.apk Cubebook-android-${{ inputs.version }}-universal.apk

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: android-artifacts
          path: build/app/outputs/flutter-apk/*.apk
          if-no-files-found: error
