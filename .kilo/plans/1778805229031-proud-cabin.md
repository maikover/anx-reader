# Plan: Corregir barra de búsqueda en pantalla principal (tema claro)

## Problema
La barra de búsqueda en la pantalla principal (`bookshelf_page.dart` líneas 327-360) usa `FilledContainer` con `useNeoStyle = true`, lo que le aplica:
- Borde sólido de 4px color `ink` (negro)
- Sombras neo-brutalistas duras

Esto se ve "oscuro" en tema claro, especialmente comparado con otros campos de texto como los de `SearchPage` que usan `InputDecorationTheme` del tema.

## Análisis
- **Ubicación**: `lib/page/home_page/bookshelf_page.dart:327-360`
- **Widgetproblemático**: `FilledContainer` con `useNeoStyle = true`
- **Alternativa correcta**: `TextField` con `InputDecoration` que usa el tema del app

## Solución
Reemplazar el `InkWell` + `FilledContainer` por un `TextField` con `InputDecoration` que:
1. Use el estilo del tema (bordes sutiles, sin sombras duras)
2. Mantenga la funcionalidad de navegación a `SearchPage` al tocar
3. Use el `prefixIcon` de búsqueda y hintText apropiado

## Cambios a realizar
En `lib/page/home_page/bookshelf_page.dart`:

Reemplazar (líneas 328-360):
```dart
Expanded(
  child: Container(
    height: 40,
    constraints: const BoxConstraints(maxWidth: 400),
    child: InkWell(
      onTap: () {
        Navigator.of(context).push(
          MaterialPageRoute(
            builder: (_) => const SearchPage(),
          ),
        );
      },
      child: FilledContainer(
        padding: const EdgeInsets.symmetric(horizontal: 12),
        color: Theme.of(context).colorScheme.surface.withAlpha(80),
        child: Row(
          children: [
            const Icon(Icons.search, color: Colors.grey),
            const SizedBox(width: 8),
            Expanded(
              child: Text(
                L10n.of(context).searchBooksOrNotes,
                style: Theme.of(context)
                    .textTheme
                    .bodyMedium
                    ?.copyWith(color: Theme.of(context).hintColor),
                overflow: TextOverflow.ellipsis,
              ),
            ),
          ],
        ),
      ),
    ),
  ),
),
```

Por:
```dart
Expanded(
  child: ConstrainedBox(
    constraints: const BoxConstraints(maxWidth: 400),
    child: SizedBox(
      height: 40,
      child: TextField(
        readOnly: true,
        onTap: () {
          Navigator.of(context).push(
            MaterialPageRoute(
              builder: (_) => const SearchPage(),
            ),
          );
        },
        decoration: InputDecoration(
          hintText: L10n.of(context).searchBooksOrNotes,
          prefixIcon: const Icon(Icons.search),
          border: OutlineInputBorder(
            borderRadius: BorderRadius.circular(24),
          ),
          contentPadding: const EdgeInsets.symmetric(horizontal: 12, vertical: 8),
        ),
      ),
    ),
  ),
),
```

**Nota**: Se usa `ConstrainedBox` en lugar de `Container` porque `SizedBox` no acepta el parámetro `constraints`. Se mantiene el `maxWidth: 400` para limitar el ancho máximo de la barra de búsqueda.

**IMPORTANTE**: No eliminar el import de `FilledContainer` porque se usa en otra parte del archivo (línea 132) para el menú de tags.

## Validación
- En tema claro: la barra debe verse con colores claros, sin sombras oscuras
- En tema oscuro: la barra debe adaptarse correctamente al tema oscuro
- La funcionalidad de navegación a SearchPage debe mantenerse